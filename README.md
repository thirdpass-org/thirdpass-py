# vouch-py

Python package extension for Vouch.

This repo contains the Vouch extension that understands Python packages and
Python dependency files. It can be used by the Vouch CLI to discover Python
dependencies and fetch package metadata from PyPI.
