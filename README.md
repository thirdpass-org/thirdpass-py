# thirdpass-py

Python package extension for Thirdpass.

This repo contains the Thirdpass extension that understands Python packages and
Python dependency files. It can be used by the Thirdpass CLI to discover Python
dependencies and fetch package metadata from PyPI.
